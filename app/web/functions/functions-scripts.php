<?php
declare(strict_types=1);

/**
 * functions-scripts.php
 * - 공통 함수/헬퍼 모음의 예시 스텁.
 * - 실제 서비스 로직에 따라 이 파일에 함수들을 채워 넣거나, 다른 파일명으로 교체 가능.
 */

if (!function_exists('hc_helper_loaded')) {
    function hc_helper_loaded(): bool
    {
        return true;
    }
}
